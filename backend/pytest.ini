[pytest]
addopts = --cov=app --cov-report=term-missing:skip-covered --cov-report=xml:coverage.xml
# Ensure both current directory (backend root) and app package are on sys.path
pythonpath = . app
filterwarnings = ignore::DeprecationWarning
