# TV v13 Windows server qualification and activation

14 September 2026, 15:51 +08. The user authorized updating the existing TV server
for the staged v13 APK. Active implementation:
`a72aa320801787cd066c6e04c33764e9271c9ab5`.
All 641 staged Git files matched their SHA-256 pins before activation and again
before marking the APK ready. Private routing, paths, source indexes and runtime
receipts are retained outside this public repository.

## Qualification under the actual service account

A temporary no-listener qualification task used the existing Limited Interactive
Windows principal and a separate cold cache, with a 2 GiB committed-memory Job
Object and no console. Native tests: 30 run, 29 passed, one symlink-creation case
skipped because Limited Windows lacks that privilege. That exact synthetic case
passed separately under the operator identity. No application test source changed.

Real-config ASGI qualification covered all 27,842 selected catalog entries over
279 pages, checking ready-first order, uniqueness, completeness and filter counts.
Ready admission: 23,588 of 23,599 photos and 2 of 4,243 videos. Finished but
unpublished batch outputs remain excluded. Counts are delivery admission, not a
claim of checking every media file or device decoder.

Two previously failing photos passed cold grid/display generation, identical
cache hits and original byte ranges. Cold requests took 0.672–0.734 seconds.
Both currently published prepared videos passed HEAD and start/middle/end ranges.
Invalid selection, stale revision, denied peers, credential rejection and legacy
no-browse behavior passed. Positive media requests used the real Limited principal
through ASGI, not a remote projector HTTPS connection.

## Activation and independent live checks

Only the existing TV v3 task was switched, preserving its principal, zero automatic
triggers, origin, allowed peers, TLS, catalog/control, source index and live cache.
The new private pythonw bootstrap applies a 2 GiB Job Object limit to the service
and descendants before startup, with an 8 GiB available-RAM startup check. This
startup check is not a continuous free-RAM watchdog. No console was opened.

The first attempt hit an operator-script error reading serialized task arguments;
the saved task definition successfully restored the previous service. The corrected
script captures validated strings directly from the live task before stopping it.
The second activation passed source/input/listener/receipt checks. The exact old
task definition and source are retained for scoped rollback; no broad cleanup or
firewall rollback script was used.

Live trusted TLS passed, and the actual operator peer correctly received HTTP 403.
The operator network is outside the existing allowed audience. No audience expansion
or source-IP bypass was used; allowed-peer HTTPS and physical TV acceptance remain
for the user's projector test. At 15:51 the service used 68,124,672 bytes RSS and
100,388,864 private bytes, with 51,895,672,832 bytes physical RAM available. Logs
were bounded. Unrelated listener identities and publication hashes were unchanged.

Captioning and the GPU video batch remained running; their tasks were not restarted.
The private publication continuation was updated to preserve the v13 browse contract,
fixed Windows memory sampler and service memory wrapper on subsequent publication.
Preparation progress is separate from published readiness and decoder acceptance.

## Install artifact and remaining gates

`PhotoHouse-TV-v13-home.apk1` is ready in the existing `ProjectorMedia/APKs` folder.
It is the same configured v13 binary previously staged as pending-server:
10,449,814 bytes, SHA-256
`ebfc3a58013bd83a762ea34eeb2ad6cfb0768a76b04b8dffb754c6c244c89d8f`.
SMB readback under the operator identity matched; reader-group read/execute ACL
was checked. Older v10/v11/v12 binaries were preserved byte-for-byte. Obsolete
pending notes were archived privately and replaced by current install instructions.

The user will install/update v13 later. If `.apk1` is not recognized, copy it to
projector Downloads and rename the local copy to `.apk`; do not uninstall first.
Check D-pad selection, pagination, thumbnails, photo opening and video playback.
Additional video publication and protected-phone readiness parity remain separate
work. No physical installation, all-library playability or phone parity is claimed.
