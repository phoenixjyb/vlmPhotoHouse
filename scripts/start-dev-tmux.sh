#!/usr/bin/env bash
# Retired in the protected-access release; never start the legacy server or models.
printf '%s\n' 'Legacy combined launcher retired. Use scripts/staging_app.py with explicit private configuration after following docs/security/PROTECTED_UPGRADE.md. No runtime action was performed.' >&2
exit 2
